<PageHeader @iconName="folder-outline">
  <h1>{{@model.name}}</h1>
  <small>Project metadata</small>
</PageHeader>

<aside>
  <LinkTo @route="workspace.projects.project.apps" @models={{array "default" @model.name }}>
    {{t "nav.application_index"}}
  </LinkTo>
</aside>

{{outlet}}
