<h5>Applications for {{@model.project.project}}</h5>

<ul>
    {{#each @model.applications as |app|}}
    <li>App: <LinkTo @route="workspace.project.app" @model={{app.name}}>{{app.name}}</LinkTo>
    </li>
    {{/each}}
</ul>

{{outlet}}