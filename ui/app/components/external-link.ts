import Component from '@glimmer/component';

/**
 *
 * `ExternalLink` renders an anchor tag with the proper `rel` attribute and the
 *    external link "exit" icon.
 *
 *
 * ```
 * <ExternalLink href="https://www.hashicorp.com">
 *   Some Link Text
 * </ExternalLink>
 * ```
 *
 * @class ExternalLink
 *
 */

export default class ExternalLinkComponent extends Component {}
