<pre><code class="cmd">waypoint context create \
    -server-addr={{this.hostname}} \
    -server-auth-token={{this.token}} \
    -server-require-auth=true \
    -set-default {{this.contextName}}</code></pre>