<b class="badge-status badge-status--{{this.statusClass}}">
  <Pds::Icon @type={{this.iconType}}/>
  {{#unless this.iconOnly}}
    {{t (concat 'status_badge.' this.statusClass)}}
    {{yield}}
  {{/unless}}
</b>
