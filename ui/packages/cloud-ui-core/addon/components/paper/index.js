import Component from '@glimmer/component';

/**
 *
 * `Paper` is the white container wrapping elements.
 *
 *
 * ```
 * <Paper @variant="outlined" @square={{true}}></Paper>
 * ```
 *
 * @class Paper
 *
 */

export default class PaperComponent extends Component {
  /**
   * Gives the container a different style, like "outlined".
   * @argument variant
   * @type {?string}
   */
  /**
   * Removes the container's rounded edges.
   * @argument square
   * @default false
   * @type {?boolean}
   */
}
