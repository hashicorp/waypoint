// Package vcsutil provides utility functions for inspecting the state of a local git repository.
//
// Currently, vcsutil executes the `git` command, so git must be installed for it to work.
package vcsutil
