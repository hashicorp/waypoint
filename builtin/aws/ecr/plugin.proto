syntax = "proto3";

package ecr;

option go_package = "waypoint/builtin/aws/ecr";

// Image is the artifact type for the registry.
message Image {
  string image = 1;
  string tag = 2;
  string architecture = 3;
}
