## files (registry)

Copies files to a specific directory.

### Interface

### Variables

#### path

The filesystem path to store the files into.

- Type: **string**

### Examples

```

build {
  use "files" {}
  registry {
	use "files" {
	  path = "/path/to/file"
	}
  }
}

```
