{
  "scripts": {
    "start": "cd website-preview && npm run start",
    "dev": "npm run start",
    "clean": "rm -rf ./docs-preview",
    "generate:readme": "next-hashicorp markdown-blocks README.md",
    "format": "next-hashicorp format content/**/* data/**/*",
    "lint": "next-hashicorp lint content/**/* data/**/*",
    "vercel-build": "npm i -g npm@7 && ./scripts/website-preview-setup.sh && rm -rf .next && cd ./website-preview && DEV_IO_PROXY=waypoint npm run build && cp -R .next/ ../.next"
  },
  "devDependencies": {
    "@hashicorp/platform-cli": "^1.2.0"
  },
  "dependencies": {
    "next": "^11.1.2"
  }
}
