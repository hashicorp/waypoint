## Context

One of the functions of install is that it creates a context automatically that points to the newly
created server with authentication. This is how waypoint knows the address of the server when you run
later waypoint commands.
